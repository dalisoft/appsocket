{
  "private": true,
  "name": "websocket-osx-app",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-synacor": "^3.0.4",
    "if-env": "^1.0.4",
    "preact-cli": "^2.2.1",
    "@namics/stylelint-bem": "^6.0.0",
    "husky": "^2.3.0",
    "lint-staged": "^8.1.7",
    "prettier": "^1.17.1",
    "stylelint": "^10.0.1"
  },
  "dependencies": {
    "preact": "^8.4.2",
    "classnames": "^2.2.6",
    "unistore": "^3.4.1"
  }
}
